services:
  - type: web
    name: penda-foundation
    runtime: node
    plan: free
    region: oregon
    branch: master
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "22.12.0"